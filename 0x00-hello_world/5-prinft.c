#include <stdio.h>

/**
 * main - prints string with printf
 *
 * Return: 0 on success
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}

